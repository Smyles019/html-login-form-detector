#LogisticRegression

from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler

# Prepare target variable
y = X_preprocessed['label']

# Drop meta columns and label from X_preprocessed to get only structural features
X_struct_dense = X_preprocessed.drop(columns=['url', 'sha256', 'label'])

# Scale the dense structural features
scaler = StandardScaler()
X_struct_scaled = scaler.fit_transform(X_struct_dense)

# Combine scaled structural features (dense) and N-gram features (sparse)
X_combined_features = sparse.hstack([X_struct_scaled, X_ngram_sparse])

# Split data into training and testing sets
X_train, X_test, y_train, y_test = train_test_split(
    X_combined_features, y, test_size=0.2, random_state=42, stratify=y
)

print(f"Shape of X_train: {X_train.shape}")
print(f"Shape of X_test: {X_test.shape}")
print(f"Shape of y_train: {y_train.shape}")
print(f"Shape of y_test: {y_test.shape}")

# Display the first 5 rows of scaled structural features (as DataFrame for readability)
# Note: X_combined_features will be a sparse matrix, so we'll inspect the scaled dense part.
display(pd.DataFrame(X_struct_scaled, columns=X_struct_dense.columns).head())
